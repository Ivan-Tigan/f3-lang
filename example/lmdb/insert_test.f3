// Simple LMDB test - insert only
(
    ?WriteTxn 
        lmdbStartRW "./lmdb_data";
        lmdbInsert { alice likes bob. };
        lmdbEnd [].
) => (
    result inserted ok.
).
system debug listing.
system query [ ?X ( result inserted ?X. ) ].